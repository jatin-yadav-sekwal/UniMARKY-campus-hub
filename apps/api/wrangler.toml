name = "unimarky-campus-hub"
main = "server/app.ts"
compatibility_date = "2024-02-08"
compatibility_flags = ["nodejs_compat"]

[vars]
# Public variables here if any

# Secrets like DATABASE_URL should be set via `wrangler secret put DATABASE_URL`
